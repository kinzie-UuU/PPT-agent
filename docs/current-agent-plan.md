# PPT Agent 当前状态与后续计划

更新时间：2026-06-30

## 目标

把 `E:\PPT工具` 做成围绕两套 Skill 的本地 PPT Agent：

```text
上传 PPT / PDF / 图片 / brief
-> codex-ppt 生成视觉统一的图片型 PPT
-> image-to-editable-ppt / editppt 重建为对象级可编辑 PPT
-> 质量检查、人工复核、失败恢复、交付门禁
-> 下载 editable-final.pptx
```

旧模板、旧设计方向和旧本地一键生成器不回到主流程，只能作为高级诊断或兼容提示。

## 当前状态

- 服务地址：`http://127.0.0.1:4180/`
- 当前真实 job：`workflow_20260629-021146Z_619d82`
- 源文件：`汇川中秋--20260611.pptx`
- 源页数：20 页
- 图片模型：`gpt-image-2`
- OCR：`paddleocr-local`，`rapidocr-local` fallback
- 已完成图片型页面：20/20 页
- 已完成图片型 PPT：20/20 页
- 已完成 OCR / 文字提示：20/20 页
- 已完成可编辑重建：2/20 页
- 当前最终文件：`workspace/jobs/workflow_20260629-021146Z_619d82/final/editable-final.pptx`
- 当前交付状态：blocked，完整产品交付未完成
- 当前下一步：继续剩余 18 页 `image-to-editable-ppt / editppt` 页面重建
- 当前待重建页集：`page_001,page_002,page_005-page_020`
- 当前完整页面证据页：`page_003,page_004`
- 注意：当前 2 页 final 是旧小样本结果，不等于当前已记录页面证据页；最终必须重新合成 20/20。
- 剩余页预计调用：144 次 `gpt-image-2` 图片 API，启动前必须显式授权

当前已确认的基础能力：

- `codex-ppt` 已完成 20 页真实图片型页面。
- 图片型 PPT 已覆盖源文件 20 页。
- OCR 使用本地开源 PaddleOCR，乱码计数为 0。
- `image-to-editable-ppt / editppt` 已有 2 页完整页面证据。
- 已支持逐页人工复核标记；当前仅记录了 1 页“暂时接受”，还未完成最终人工复核门禁。
- 当前人工复核标记仍是阶段性记录；20/20 最终文件生成后必须重新做最终人工复核。

当前判断：

- 这不是完整 20 页产品级交付，只是“20 页图片型 PPT + 2 页可编辑重建”的阶段性结果。
- 真正阻断点是剩余 18 页可编辑重建和最终交付证据。
- 继续前必须明确外部 API 额度范围，不能静默跑全量。

## 后续执行顺序

1. **下一步提示收口**
   - `/next` 在 2/20 final 状态下必须返回“复核当前 2 页样例”和“继续生成剩余 18 页”。
   - 交付区必须明确当前是 draft，不是完整 ready。

2. **继续剩余页面**
   - 用户明确确认额度后，继续处理 page_001、page_002、page_005 到 page_020。
   - 分批运行 `image-to-editable-ppt / editppt` 页面重建。
   - 失败页单独重跑，不污染已成功页面。

3. **20 页产品级验收**
   - 最终合成 20 页 `editable-final.pptx`。
   - 检查 PowerPoint 可打开。
   - 检查对象级可编辑性。
   - 检查无整页截图冒充。
   - 检查页面证据和最终证据完整。
   - 记录人工视觉复核。
   - 交付门禁必须达到 ready。

4. **普通用户前端继续收口**
   - 首页只保留用户能理解的阶段、阻断原因、下一步和最终文件状态。
   - worker、manifest、hash、artifact、provider 默认进入高级详情。
   - 旧模板、旧设计方向、旧一键生成器不显示在主流程。

5. **失败恢复**
   - 明确失败原因：模型无响应、JSON 不可解析、额度不足、鉴权失败、PowerPoint 打开失败、页面证据过期、整页截图风险。
   - 明确影响页面。
   - 提供重置页面、重跑页面、重新合成 final。

## 不要做

- 不要恢复旧模板主入口。
- 不要用 passthrough 或 dry-run 冒充产品级结果。
- 不要绕过交付门禁直接开放完整交付。
- 不要在用户未确认额度前自动跑全量外部 API。
- 不要把 `codex-ppt` 和 `image-to-editable-ppt` 的核心契约改成新的平行流程。
## 2026-06-30 增量状态补充

- 前端已固定当前主验收任务：`workflow_20260629-021146Z_619d82`，源文件 `汇川中秋--20260611.pptx`，源页数 20，当前可编辑 final 为 2/20 小样本。
- 交付中心已区分两个下载入口：
  - `final-pptx`：最终产品级下载，当前仍被交付门禁阻断，接口返回 409。
  - `draft-final-pptx`：当前小样本草稿下载，文件名 `editable-sample-2p-draft.pptx`，只用于测试当前 2 页范围，不能作为完整产品交付。
- 继续剩余页已改成分批受控启动：默认先跑 2 页，可选择 1 页、2 页、5 页或剩余全部；预检、额度授权、启动都会围绕本批页面，不再默认静默启动 18 页。
- 页面 worker 运行状态已在普通工作台显示：最近一批状态、页码、成功数、失败数、低复杂度模式和日志入口都会展示；运行中会自动刷新。
- 失败页恢复已形成用户可理解链路：重置失败页、重跑前预检、只重跑影响页、重新合成 final。`page_005` 最近失败原因是页面重建模型超时，预检会自动进入低复杂度页面规格模式。
- 失败页恢复接口已补充 `recoveryPlan`：前端会展示失败页、失败类型、成功页保护、低复杂度建议、是否自动合成，以及“查看失败原因 -> 重置失败页 -> 重跑前预检 -> 只重跑影响页 -> 重新合成 final”的步骤。
- 人工复核记录已区分 `sample` / `full` 范围：当前 2/20 只能作为小样本复核，不能解锁完整产品交付；20/20 最终文件生成后必须重新记录完整复核。
- 普通用户工作台的 5 步主流程已补充细节状态：`转成可编辑 PPT` 显示 2/20，`人工复核` 区分“小样本已复核/完整复核已记录”，`下载最终 PPT` 区分“仅小样本草稿/最终产品级”。
- 当前仍未完成完整 20/20 可编辑交付；继续真实运行前必须显式确认外部 API 额度和模型恢复状态。
